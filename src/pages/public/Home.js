import { Container, Typography } from "@mui/material";
import React from "react";

const Home = () => {
    return (
        <Container maxWidth="sm">
            <Typography>Please Sign In or Sign Up</Typography>
        </Container>
    );
};

export default Home;
